<p id="notice"><%= notice %></p>
<p>
  <strong>Name project:</strong>
  <%= @project.name_project %>
</p>

<p>
  <strong>Start date project:</strong>
  <%= @project.start_date_project %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<%= link_to 'Novo objetivo de medição', new_goal_path(:project_id => @project.id) %>
<%= link_to 'Ver os objetivos desse projeto', goals_path(:project_id => @project.id) %>
<%= link_to 'Editar', edit_project_path(@project) %> |
<%= link_to 'Voltar', projects_path %>
