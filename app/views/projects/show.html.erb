<p id="notice"><%= notice %></p>
<p>
  <strong>Name project:</strong>
  <%= @project.name_project %>
</p>

<p>
  <strong>Start date project:</strong>
  <%= @project.start_date_project %>
</p>

<p>
  <strong>Description:</strong>
  <%= @project.description %>
</p>

<%= link_to 'New measurement goal', new_goal_path(:project_id => @project.id) %>
<%= link_to 'See project goals', goals_path(:project_id => @project.id) %>
<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
