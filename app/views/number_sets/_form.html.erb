<%= form_for(@number_set) do |f| %>
  <% if @number_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@number_set.errors.count, "error") %> prohibited this number_set from being saved:</h2>

      <ul>
      <% @number_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_number_set %><br>
    <%= f.text_field :name_number_set %>
  </div>
  <div class="field">
    <%= f.label :description_number_set %><br>
    <%= f.text_area :description_number_set %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
